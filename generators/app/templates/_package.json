{
  "name": "<%= appname %>",
  "version": "0.0.1",
  "description": "<%= description %>",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "make"
  },
  "repository": {
    "type": "git",
    "url": "https://<%= repo_base %>/<%= repo_user %>/<%= appname %>"
  },
  "author": {
    "name": "<%= realname %>",
    "email": "<%= email %>"
  },
  "license": "<%= license %>",
  "preferGlobal": <%= preferGlobal %>,
  "bin": {
    "<%= appname %>": "bin/<%= appname %>"
  },
  "man": [
    "man/<%= appname %>.1.gz"
  ],
  "bugs": {
    "url": "https://<%= repo_base %>/<%= repo_user %>/<%= appname %>/issues"
  },
  "dependencies": {
  }
}
